[user]
        name = {{ .name }}
        email = {{ .email }}
        signingkey = {{ .signingkey }}
[pull]
        ff = only
[commit]
        gpgsign = true
[tag]
        gpgsign = true        
[gpg]
        format = ssh